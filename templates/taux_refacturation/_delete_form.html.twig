<form method="post" action="{{ path('taux_refacturation_delete', {'id': taux_refacturation.id}) }}" onsubmit="return confirm('Êtes-vous sûre de vouloir supprimer ce donnée?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ taux_refacturation.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
