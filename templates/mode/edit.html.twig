{% extends 'base.html.twig' %}

{% block title %}Edition Mode{% endblock %}

{% block body %}
<div class="container">
    <div class="row">
        <h1>Edition Mode</h1>
    </div>
    <hr>

    {{ include('mode/_form.html.twig', {'button_label': 'Modifier'}) }}
    <br>
    <a href="{{ path('mode_index') }}" class="btn btn-primary">Retour</a>
    <br><br>
    {{ include('mode/_delete_form.html.twig') }}
</div>
{% endblock %}
