{% extends 'base.html.twig' %}

{% block title %}Mouvement index{% endblock %}

{% block body %}
    <h1>Mouvement index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Mouvement</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for mouvement in mouvements %}
            <tr>
                <td>{{ mouvement.id }}</td>
                <td>{{ mouvement.mouvement }}</td>
                <td>
                    <a href="{{ path('mouvement_show', {'id': mouvement.id}) }}">show</a>
                    <a href="{{ path('mouvement_edit', {'id': mouvement.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('mouvement_new') }}">Create new</a>
{% endblock %}
