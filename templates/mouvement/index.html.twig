{% extends 'base.html.twig' %}

{% block title %}Liste Mouvement{% endblock %}

{% block body %}
<div class="container">
    <div class="row">
        <h1>Liste Mouvement</h1>
    </div>
    <hr>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Mouvement</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for mouvement in mouvements %}
            <tr>
                <td>{{ mouvement.id }}</td>
                <td>{{ mouvement.mouvement }}</td>
                <td>
                    <a href="{{ path('mouvement_show', {'id': mouvement.id}) }}" class="btn btn-warning">voir</a>
                    <a href="{{ path('mouvement_edit', {'id': mouvement.id}) }}" class="btn btn-danger">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">Aucun enregistrement trouver!</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <div class="navigation">
        {{ knp_pagination_render(mouvements) }}
    </div>

    <a href="{{ path('mouvement_new') }}" class="btn btn-success">Créer</a>
</div>
{% endblock %}
