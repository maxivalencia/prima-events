{% extends 'base.html.twig' %}

{% block title %}Liste Stock{% endblock %}

{% block body %}
<div class="container">
    <div class="row">
        <h1>Détails historique</h1>
    </div>
    <hr>

    <table class="table">
        <thead>
            <tr>
                <th>Quantite</th>
                <th>Client</th>
                <th>Reference</th>
                <th>Date Commande</th>
                <th>Date Sortie Prevue</th>
                <th>Date Sortie Effectif</th>
                <th>Date Retour Prevu</th>
                <th>Date Retour Effectif</th>
            </tr>
        </thead>
        <tbody>
        {% for stock in stocks %}
            <tr>
                <td>{{ stock.quantite }}</td>
                <td>{{ stock.client.nom }}</td>
                <td>{{ stock.reference }}</td>
                <td>{{ stock.dateCommande ? stock.dateCommande|date('Y-m-d') : '' }}</td>
                <td>{{ stock.dateSortiePrevue ? stock.dateSortiePrevue|date('Y-m-d') : '' }}</td>
                <td>{{ stock.dateSortieEffectif ? stock.dateSortieEffectif|date('Y-m-d') : '' }}</td>
                <td>{{ stock.dateRetourPrevu ? stock.dateRetourPrevu|date('Y-m-d') : '' }}</td>
                <td>{{ stock.dateRetourEffectif ? stock.dateRetourEffectif|date('Y-m-d') : '' }}</td>
            </tr>
        {% else %}
            <tr>
                <td colspan="9">Aucun enregistrement trouver!</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    <br>
    <a href="{{ path('historique') }}" class="btn btn-success">Retour</a>
</div>
{% endblock %}
