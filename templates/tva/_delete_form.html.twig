<form method="post" action="{{ path('t_v_a_delete', {'id': t_v_a.id}) }}" onsubmit="return confirm('Êtes-vous sûre de vouloir supprimer ce donnée?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ t_v_a.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
