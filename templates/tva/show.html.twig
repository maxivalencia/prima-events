{% extends 'base.html.twig' %}

{% block title %}TVA{% endblock %}

{% block body %}
    <h1>TVA</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ t_v_a.id }}</td>
            </tr>
            <tr>
                <th>Tva</th>
                <td>{{ t_v_a.tva }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('t_v_a_index') }}">back to list</a>

    <a href="{{ path('t_v_a_edit', {'id': t_v_a.id}) }}">edit</a>

    {{ include('t_v_a/_delete_form.html.twig') }}
{% endblock %}
