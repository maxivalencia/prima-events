<form method="post" action="{{ path('motif_payement_delete', {'id': motif_payement.id}) }}" onsubmit="return confirm('Êtes-vous sûre de vouloir supprimer ce donnée?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ motif_payement.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
