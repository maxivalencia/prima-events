{% extends 'base.html.twig' %}

{% block title %}Paye{% endblock %}

{% block body %}
    <h1>Paye</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ paye.id }}</td>
            </tr>
            <tr>
                <th>Refstock</th>
                <td>{{ paye.refstock }}</td>
            </tr>
            <tr>
                <th>DatePayement</th>
                <td>{{ paye.datePayement ? paye.datePayement|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>TVA</th>
                <td>{{ paye.TVA ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('paye_index') }}">back to list</a>

    <a href="{{ path('paye_edit', {'id': paye.id}) }}">edit</a>

    {{ include('paye/_delete_form.html.twig') }}
{% endblock %}
