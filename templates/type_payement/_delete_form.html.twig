<form method="post" action="{{ path('type_payement_delete', {'id': type_payement.id}) }}" onsubmit="return confirm('Êtes-vous sûre de vouloir supprimer ce donnée?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_payement.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
