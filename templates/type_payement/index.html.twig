{% extends 'base.html.twig' %}

{% block title %}Liste Type de Payement{% endblock %}

{% block body %}
<div class="container">
    <div class="row">
        <h1>Liste Type de Payement</h1>
    </div>
    <hr>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Type</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for type_payement in type_payements %}
            <tr>
                <td>{{ type_payement.id }}</td>
                <td>{{ type_payement.type }}</td>
                <td>
                    <a href="{{ path('type_payement_show', {'id': type_payement.id}) }}" class="btn btn-warning">voir</a>
                    <a href="{{ path('type_payement_edit', {'id': type_payement.id}) }}" class="btn btn-danger">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">Aucun enregistrement trouver!</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <div class="navigation">
        {{ knp_pagination_render(type_payements) }}
    </div>

    <a href="{{ path('type_payement_new') }}" class="btn btn-success">Créer</a>
</div>
{% endblock %}
